type Query {
  search(text: String!): SearchResult
}

type Mutation {
  say_hello(name: String!): String!
}

type SearchResult {
  answer: String!
  documents: [Document!]!
}
type Document {
  id: String!
  text: String!
}
